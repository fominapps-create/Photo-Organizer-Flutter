import 'package:flutter/material.dart';

class AppColors {
  // Sparkle color used for the FAB across screens. Chosen to match the
  // darker amber tone used in the Gallery when folders are selected.
  // Sparkle color used for the FAB across screens. Switched to purple
  // per user preference.
  static const Color sparkle = Color(0xFF7B1FA2); // Purple 700
  // Slightly darker variant used when folders are selected.
  static const Color sparkleDark = Color(0xFF512DA8); // Deep Purple 700
}
