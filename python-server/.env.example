# Example environment variables for python-server
# Copy this file to `.env` and update values for your machine.

# Where the server should place organized images (absolute path). Keep outside project if you don't want uploaded files in repo.
TARGET_FOLDER=C:\Users\MIKE\Pictures\Organized

# If set, the server will require this upload token (optional)
UPLOAD_TOKEN=

# If true, server keeps uploaded files after processing (default: False)
PERSIST_UPLOADS=False

# Allow remote binding (0.0.0.0) - set to True if you want other devices on LAN to access server
ALLOW_REMOTE=False

# Optional comma-separated CORS origins
ALLOW_ORIGINS=

# Temp folder used by server
TEMP_FOLDER=temp
